<UserControl
    x:Class="XWingSquadronBuilder_v4.UserControls.Squadron"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"    
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!--<UserControl.Resources>
        <converters:PilotToPilotViewModelConverter x:Name="IPilotToPilotViewModelCoverter"/>
    </UserControl.Resources>

    <Grid>
        <ItemsControl x:Name="PilotList" ItemsSource="{x:Bind ViewModel.Squadron.Pilots, Mode=OneWay}">
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="interface:IPilot">
                    <Grid>
                        <userControl:PilotControl Pilot="{x:Bind}" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>-->
</UserControl>
